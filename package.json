{
  "name": "verbis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "dev:mobile": "pnpm --filter @verbis/mobile dev",
    "dev:backend": "pnpm --filter @verbis/backend dev",
    "build": "pnpm run -r build",
    "lint": "pnpm run -r lint",
    "clean": "pnpm run -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "packageManager": "pnpm@10.11.0"
}